import { makeStyles } from "@mui/styles";

export const Style = makeStyles((theme) => {
  return {
    container: {
      width: "100%",
      height: "600px",
      backgroundColor: "#FF6F00",
      fontSize: "small",
    },
  };
});
